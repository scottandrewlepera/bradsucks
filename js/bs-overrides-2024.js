/*
JS used to override page styles. Each must be wrapped in a <script> tag to work.
To apply, open the page in the SQS editor, then use Settings > Advanced > Inject custom code.
*/

// Latest (linktree)
document.addEventListener("DOMContentLoaded", (event) => {document.body.classList.add("bs-linktree");});

